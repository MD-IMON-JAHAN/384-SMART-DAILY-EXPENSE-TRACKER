<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Dark theme -->
    <style name="Theme.SmartDailyExpenseTracker" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color (adjust for dark mode readability) -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/purple_500</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/teal_500</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>

        <!-- Toolbar and Action Bar colors -->
        <item name="actionBarTheme">@style/ThemeOverlay.AppCompat.Dark</item>
        <item name="toolbarStyle">@style/Widget.AppCompat.Toolbar</item>

        <!-- Menu and Overflow colors for dark theme -->
        <item name="android:textColorPrimary">@color/text_color_primary</item>
        <item name="android:textColorSecondary">@color/text_color_secondary</item>
        <item name="actionMenuTextColor">@color/text_color_primary</item>
        <item name="android:itemTextAppearance">@style/MenuTextAppearanceDark</item>

        <!-- Customize your theme here -->
        <item name="android:windowBackground">@color/black</item>
    </style>

    <!-- Menu text appearance for dark theme -->
    <style name="MenuTextAppearanceDark" parent="TextAppearance.AppCompat.Menu">
        <item name="android:textColor">@color/text_color_primary</item>
    </style>
</resources>